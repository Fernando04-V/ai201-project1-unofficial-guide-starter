sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface — add whichever you use for Milestone 5:
# Gradio 6.x requires huggingface-hub>=1.2, which conflicts with the
# sentence-transformers/transformers stack above (needs hub<1.0). Gradio 5.x
# is compatible, so we pin to it.
gradio>=5.0,<6
# streamlit>=1.40.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
